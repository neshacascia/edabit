// Miserable Parody of a Calculator:
// Create a function that will handle simple math expressions. The input is an
// expression in the form of a string.

function calculator(str) {
  return eval(str);
}
