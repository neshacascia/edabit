// Buggy Code:
// The challenge is to try and fix this buggy code, given the inputs true and false.
// See the examples below for the expected output.

function hasBugs(buggy_code) {
  return buggy_code ? 'sad days' : "it's a good day";
}
