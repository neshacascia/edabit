// You need to create two functions to substitute toString() and parseInt(); A function called
// intToString() that converts integers into strings and a function called stringToInt() that
// converts strings into integers.

// You will get bonus points if you manage to solve this without using toString, parseInt or parseFloat.

function intToString(num) {
  return `${num}`;
}

function stringToInt(num) {
  return Number(num);
}
